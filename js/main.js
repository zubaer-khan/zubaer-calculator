class Calculator{
    constructor (getSum) {
        this.sum = getSum;
    }
        add(){
        let num = document.querySelector('button').addEventListener('click').value;
        console.log(num)
        }
    
}
